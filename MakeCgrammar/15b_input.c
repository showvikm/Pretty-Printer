int fucntion()
{
    double iterator, times;
    iterator = 0;
    scanf("%d", &times);
    while (iterator > times)
    {
        iterator++;
        printf("%d\n", iterator);
    }
    return 10000;
}